#!/bin/bash

sudo apt update
sudo apt install -y fzf

cd $HOME

if [ ! -f .bashrc_extender ]; then
  echo "source .bashrc_extender" >> .bashrc
fi 

wget https://raw.githubusercontent.com/jaydabi/bash-config/main/.bashrc_extender

exit 0
